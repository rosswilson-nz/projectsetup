---
title: 'Paper title'
author:
  - Author list (one per line)
date: last-modified
date-format: "D MMMM YYYY"
format:
  docx:
    reference-doc: "word-styles-reference-01.docx"
    fig-width: 6
    fig-height: 8
    fig-dpi: 300
callout-appearance: minimal
bibliography: references.bib
csl: vancouver.csl
abstract: |
  **Abstract**
  
  *Background*
  
  *Aim*
  
  *Methods*
  
  *Results*
  
  *Conclusion*
---

```{r setup, include=FALSE, echo=FALSE}
knitr::opts_chunk$set(  
	collapse = TRUE,
  warning = FALSE,
  message = FALSE,
  echo = FALSE,
  comment = "#>"
)
options(knitr.kable.NA = "")

# Load required libraries
library(targets)
#library(knitr) # Uncomment if knitr::kable used to print tables
#library(flextable) # Uncomment if flextable::flextable used to print tables
#library(ftExtra) # Uncomment if ftExtra functions used with flextable for table formatting
#library(fs) # Uncomment if fs::path_wd() used to generate path to figure files
#library(formattr) # Pretty number formatting
#library(tidyverse) # For manipulating analysis objects if required

# Path to targets store (in parent directory)
tar_config_set(store = here("_targets"))

# Flextable table formatting (if required):
#set_flextable_defaults(
#  font.family = "Times New Roman", font.size = 9,
#  line_spacing = 1.15, padding.top = 0, padding.bottom = 0,
#  keep_with_next = TRUE, theme_fun = theme_booktabs,
#  post_process_docx = function(x) x %>% 
#    set_table_properties(layout = "autofit", width = 1, align = "left") %>% 
#    hline_top(border = solid, part = "all") %>% 
#    hline_bottom(border = solid) %>% 
#    colformat_md()
#)
#solid <- fp_border_default("black")
#dotted <- fp_border_default("black", "dotted")
#indent <- function(x, i = NULL, j = 1, n = 1) {
#  x %>% style(i, j, pr_c = officer::fp_cell(margin.left = 5 * n))
#}
#header_row <- function(x, i, border_style = solid) {
#  x %>%
#    merge_h_range(i, 1, length(x$col_keys)) %>%
#    style(i, pr_c = officer::fp_cell(border.top = border_style))
#}

```

```{r load-data, include=FALSE, echo=FALSE, cache=FALSE}
# Load the previously stored results
#tar_load(figure_1)
#tar_load(results)

# Any other variables to be loaded/defined in here; extract data from analysis objects if required
```

<br>

<!-- Anything else to go on the title page below title/author/abstract -->

{{< pagebreak >}}

# Introduction

# Methods

::: {.callout-important}
Add callout blocks like this
:::

# Results

```{r table-chunk-label}
#| label: tbl-label
#| tbl-cap: Table caption

# Either:
# kable(results)

# Or
# flextable(results) %>% ...
```

> Table notes

<br>

Figure from existing image file:

<!-- ![Figure 1: Figure caption](`r #path_wd(figure_1)`) -->

> Figure notes

<br>

Or, from R object:

```{r figure-chunk-label}
#| label: fig-label
#| fig-cap: Figure caption
# figure_1
```

> Figure notes

<br>

# Discussion

# Conclusion

# Funding

# Conflicts of Interest

# References

::: {#refs}
:::

# Supplementary material
